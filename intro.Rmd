---
title: "Introducing `cricinfo`"
author: "Rob and his band of merry data nerds"
date: "27/10/2017"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(cricinfo)
library(tidyverse)

```

# Get some awesome cricket data

```{r}
ElyssePerry <- fetch_player_data(275487, "T20")
MegLanning <- fetch_player_data(329336, "ODI")
SteveSmith <- fetch_player_data(267192, "Test")
ggplot(MegLanning) + geom_point(aes(x = Date, y = Score, col = NotOut)) +
ggtitle("Meg Lanning ODI Scores")


```




```{r Get data}
# Get bowling data for women's games.
# bowl_inn_data <- fetch_bowling_innings("t20", "women")
# devtools::use_data(bowl_inn_data, overwrite = T)


# Take a look.
# bowl_inn_data %>% head() %>% knitr::kable()
```

```{r}
bowl_inn_data %>% 
  ggplot(aes(x = Wkts, y = Econ)) +
  geom_point(position = "jitter", alpha = 0.3, colour = "steelblue") +
  labs(title = "Wickets vs Economy Rate",
       x = "Wickets",
       y = "Runs per over")

```

